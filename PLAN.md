# 모드별 전체 한글화 진행 계획

## 공통 작업 단위

- 사용자가 지정한 모드 하나를 한 작업 단위로 삼는다.
- 한 모드 안에서는 리소스팩, 관련 FTB Quests, 관련 KubeJS 문구를 모두 조사한다.
- 큰 언어 파일은 내부적으로 한 번에 100~200키씩 검증한다.
- FTB Quests는 관련 챕터 단위로 처리하고, 200키를 크게 넘으면 논리적인 절로 더 나눈다.
- 매 단위마다 원문 키 집합, 자리표시자, 서식 코드, JSON/SNBT 문법을 검사한다.
- 선택한 모드의 세 범위가 모두 끝나야 해당 모드 작업을 완료로 본다.

## 모드 하나를 처리하는 순서

1. **모드 범위 찾기**
   - 대상 JAR의 네임스페이스와 `en_us.json`/`ko_kr.json`을 확인한다.
   - FTB Quests의 관련 챕터·퀘스트와 KubeJS의 관련 assets·JS를 함께 찾는다.

2. **기존 번역 비교**
   - 네임스페이스와 키가 모두 일치하는 항목만 재사용 후보로 분류한다.
   - 값은 자동 채택하지 않고, 변경 원문·삭제 키·신규 키·서식 불일치를 나눈다.

3. **모드 리소스팩 번역**
   - 아이템, 블록, 기계, 메뉴, 버튼, 툴팁을 누적형 리소스팩에 번역한다.
   - 큰 파일은 100~200키씩 나눠 검사한 뒤 하나로 합친다.

4. **관련 FTB Quests 번역**
   - 실제 언어 파일 로딩 구조를 확인한다.
   - 해당 모드의 퀘스트 제목과 설명을 올바른 `ko_kr.snbt` 계열 파일에 반영한다.
   - 관련 챕터와 그룹의 목차 제목이 일반 분류명과 공식 모드명 표기 기준에 맞는지 검수한다.
   - `quest.title`이 없는 퀘스트와 `task.title`이 없는 작업의 실제 fallback 결과를 검수한다.
   - 아이템 기반 퀘스트 제목이 리소스팩의 확정 아이템 이름과 일치하는지 검사한다.
   - 아이템 `custom_name`, 영어 literal component와 설명만 번역되고 제목은 영어로 남은 항목을 검사한다.
   - 명시적인 번역 키가 없어도 실제 화면에 영어가 표시될 가능성을 확인한다.
   - 위 제목·fallback 검사를 모두 통과해야 해당 모드의 FTB Quests 번역을 완료로 판단한다.

5. **관련 KubeJS 문구 번역**
   - 언어 키로 처리할 수 있는 문구는 리소스팩에 넣는다.
   - 직접 표시 문자열은 필요한 최소 KubeJS override로 만들고 방식과 위험을 기록한다.

6. **문법 검사**
   - JSON 파싱, SNBT 구조, 키 집합, 자리표시자, 줄바꿈, 색상 코드를 검사한다.
   - 오류가 하나라도 있으면 해당 배치를 출력 폴더로 승격하지 않는다.

7. **백업과 게임 적용**
   - 덮어쓸 퀘스트·KubeJS 파일과 경로·해시를 프로젝트 `temp/backups/`에 보관한다.
   - 누적형 리소스팩과 필요한 override만 실제 인스턴스에 적용한다.
   - 적용 전후를 비교해 계획한 경로 외에는 바뀌지 않았는지 확인한다.

8. **완료 보고와 화면 검수**
   - 리소스팩 번역 수, FTB Quests 번역 수, KubeJS 번역 여부, 백업·적용 위치를 보고한다.
   - 사용자가 게임에서 메뉴, 툴팁, 퀘스트 화면의 잘림과 문맥을 확인할 수 있게 안내한다.

## ATM10 업데이트 대응

- 새 조사 매니페스트를 만들고 이전 버전과 키·원문 해시를 비교한다.
- 추가·변경·삭제된 항목만 이미 완료한 모드 단위에 다시 반영한다.
