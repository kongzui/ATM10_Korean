# ATM10 한국어 번역 프로젝트 가이드라인

## 지침 우선순위

이 문서의 규칙은 다음 우선순위를 따른다.

1. 파일 작업과 원본 보호를 위한 핵심 안전 규칙
2. 사용자가 현재 대화에서 직접 지시한 내용
3. 번역 정확성 및 게임 적용 관련 규칙
4. 프로젝트 구조, 구현, 검증 및 Git 규칙
5. 일반 작업 원칙

규칙이 충돌하면 더 구체적이고 안전한 지침을 우선한다.  
삭제와 프로젝트 외부 수정처럼 되돌리기 어렵거나 영향이 큰 작업은 명시적 지시 없이 수행하지 않는다.
실제 게임 적용은 아래의 게임 적용 기본 규칙을 따른다.

## 작업 원칙

- 작업 전에 요청 범위와 완료 조건을 짧게 정리한다.
- 요청과 직접 관련된 파일과 코드만 수정한다.
- 기존 프로젝트 구조, 스타일, 상수, 배열, 헬퍼 함수와 스크립트를 우선 재사용한다.
- 요청받지 않은 기능, 추상화, 설정 가능성, 리팩터링을 추가하지 않는다.
- 결과를 실질적으로 바꾸는 중요한 모호함만 질문한다.
- 사소한 부분은 안전한 가정을 사용하고 완료 보고에 밝힌다.
- 관련 없는 오류, 죽은 코드, 개선점을 발견해도 임의로 수정하지 않고 별도로 보고한다.
- 단순하거나 범위가 명확한 작업에는 서브에이전트를 사용하지 않는다.
- 변경된 모든 줄은 사용자의 요청과 직접 연결되어야 한다.

## 파일 작업 핵심 안전 규칙

### 실제 ATM10 인스턴스

- 실제 ATM10 인스턴스는 기본적으로 읽기 전용으로 취급한다.
- 실제 인스턴스의 파일과 폴더를 임의로 수정, 삭제, 이동하거나 이름을 바꾸지 않는다.
- `mods/`의 원본 JAR을 수정하거나 프로젝트 저장소로 복사하지 않는다.
- 월드, 로그, JAR, 캐시, 추출한 대용량 원본을 Git에 넣지 않는다.
- 완성·검증된 번역 산출물은 별도의 적용 지시를 기다리지 않고 작업 직후 실제 게임에 적용한다.
- 적용 전에는 반드시 Minecraft 또는 Java 프로세스가 실행 중인지 확인한다.
- 관련 프로세스가 실행 중이면 산출물 생성과 검증까지만 완료하고 실제 적용은 보류한다.

### 게임 적용 기본 규칙

완성·검증된 번역 산출물에는 다음 적용 작업을 허용한다.

- 적용 전에 Minecraft 또는 Java 프로세스가 실행 중인지 확인한다.
- 관련 프로세스가 실행 중이면 프로젝트 산출물 생성과 검증까지만 완료하고, 실제 ATM10 인스턴스에 대한 복사와 갱신은 수행하지 않는다.
- 실행 중이라 적용을 건너뛴 경우 완료 보고에 적용 대기 상태와 보류 사유를 명시한다.

- 완성된 리소스팩을 실제 인스턴스의 `resourcepacks/`에 복사하거나 갱신
- 검증된 FTB Quests 또는 KubeJS 관련 완성본을 필요한 대상 경로에 적용
- 적용 전 기존 대상 파일을 백업하거나 되돌릴 방법을 마련
- 적용한 파일과 경로를 완료 보고에 명시

게임 적용을 요청받았더라도 다음 항목은 수정하지 않는다.

- 원본 모드 JAR
- 월드 세이브
- 사용자 설정 중 번역과 관계없는 파일
- 로그, 캐시, 런처 또는 시스템 파일

### 프로젝트 외부 및 시스템 영역

프로젝트 작업 디렉터리 외부의 파일이나 폴더는 명시적 요청 없이 수정하거나 삭제하지 않는다.

다음 행동은 금지한다.

1. `C:\Windows`, `C:\Program Files`, AppData, Git 또는 VS Code 설치 디렉터리 수정
2. 대상 경로가 프로젝트 루트 내부인지 확인하지 않은 상태에서 파일이나 폴더 삭제
3. 명시적 요청 없이 프로젝트 외부 파일, 외부 Python 패키지 또는 시스템 바이너리 수정
4. `.git/**` 내부 파일 직접 수정
5. 원본 게임 파일을 다시 압축하거나 원본 JAR에 덮어쓰기

필요한 경우 설치된 도구, 런타임 버전, 명령 경로, 공개 인터페이스는 읽기 전용으로 확인할 수 있다.

## 번역 작업 범위

번역 대상은 원본 위치와 적용 방식에 따라 구분한다.

### 리소스팩 대상

다음 항목은 원칙적으로 `output/resourcepack/`에 누적한다.

- 모드 아이템 및 블록 이름
- 기계, 버튼, 메뉴 이름
- 일반 툴팁과 Shift 확장 툴팁
- `assets/<modid>/lang/` 형식의 언어 파일
- KubeJS가 제공하더라도 리소스팩 언어 파일로 정의된 문구

### 덮어쓰기 대상

다음 항목은 필요한 원본 구조를 확인한 뒤 `output/overrides/`에 배치한다.

- FTB Quests 챕터, 퀘스트 제목 및 설명
- `config/ftbquests/` 아래의 번역 파일
- KubeJS 스크립트나 설정 파일에 직접 작성된 사용자 표시 문구
- 리소스팩만으로 처리할 수 없는 ATM10 전용 문구

영어 문구의 출처를 추측해서 수정하지 않는다.  
해당 문구가 모드 언어 파일, FTB Quests, KubeJS 또는 다른 시스템 중 어디에서 만들어지는지 먼저 확인한다.

## 번역 작업 원칙

- 번역은 약 100~200개 항목 또는 FTB Quests 한 챕터씩 작은 작업 단위로 진행한다.
- 사용자가 특정 모드를 지정하면 그 모드의 일반 언어 파일뿐 아니라 관련 FTB Quests 및 KubeJS 문구의 존재 여부도 조사한다.
- 누락된 키만 채우지 않고, 사용자가 지정한 모드와 관련된 기존 한국어 전체를 영어 원문과 대조하여 검토한다.
- 모드 자체 `ko_kr.json`과 ATM10의 기존 `ko_kr.snbt`는 완성된 정답이 아니라 검토가 필요한 번역 후보로 취급한다.
- 기존 한국어가 있어도 의미, 용어, 검색성, 자리표시자, 문체와 게임 내 표시 결과를 검수한 뒤 사용한다.
- 기존 ATM10 5.4 번역은 참고 자료일 뿐, 정확성이나 7.1 호환성을 전제하지 않는다.
- 5.4와 7.1의 번역 키가 같아도 영어 원문이 같은지 확인한 뒤 재사용한다.
- 기존 ATM10 5.4 자동 번역은 자동 채택하지 않는다.
- 마인크래프트 본편의 공식 한국어는 명백한 오류나 프로젝트 용어 충돌이 없는 한 유지한다.
- 번역 키, JSON/SNBT 구조, 값의 자료형을 변경하지 않는다.
- `%s`, `%1$s`, `%d`, `{0}` 같은 자리표시자를 원문과 동일하게 보존한다.
- `\n`, 실제 줄바꿈, `§` 및 `&` 색상·서식 코드, 따옴표와 이스케이프 문자를 보존한다.
- 모드 이름, 아이템 고유명사, 기술 용어가 불확실하면 추측하지 않고 용어집 또는 검토 목록에 남긴다.
- 같은 용어는 프로젝트 용어집을 우선하여 일관되게 번역한다.
- 번역이 확실하지 않은 문장을 억지로 채우지 않고 검토 대상으로 표시할 수 있다.
- 문장이 사용자에게 행동을 안내하는 퀘스트 설명이라면 직역보다 실제 플레이 의미가 명확하도록 번역한다.

## 번역 문체 및 용어 결정 원칙

### 아이템·블록·기계 이름

- 멋진 표현보다 게임 내 검색성과 일관성을 우선하고, 짧은 명사형으로 작성한다.
- 영어 이름의 핵심 기술 용어와 기능 구분을 한국어에서도 드러내며, 불필요한 설명식 의역을 피한다.
- 같은 영어 용어는 특별한 문맥상 이유가 없으면 항상 같은 한국어로 번역한다.
- 서로 다른 등급이나 기능의 항목이 번역 후 같은 이름이 되지 않게 한다.
- `Upgrade`는 반드시 `업그레이드`로 번역하고 `기능`, `강화`, `개선 장치` 등으로 임의 변경하지 않는다.
- `[기능명] Upgrade`는 원칙적으로 `[기능명] 업그레이드`로 번역한다.
- `Advanced`가 등급을 뜻하면 원칙적으로 `고급`으로 번역한다.

예: `Pickup Upgrade`는 `줍기 업그레이드`, `Advanced Pickup Upgrade`는 `고급 줍기 업그레이드`로 번역한다. `고급 줍기 기능`은 사용하지 않는다.

### 툴팁과 설명

- 아이템 이름은 직역과 검색성을 우선하지만, 툴팁과 설명문은 자연스러운 한국어로 의역할 수 있다.
- 의역해도 숫자, 조건, 범위, 확률, 대상과 기능 차이를 생략하거나 바꾸지 않는다.
- 짧은 툴팁은 간결한 설명문으로 작성한다.
- Shift 확장 툴팁은 플레이어가 기능을 바로 이해할 수 있게 번역한다.

### FTB Quests

- 영어 어순을 억지로 유지하지 않고, 플레이어가 무엇을 해야 하는지 명확히 이해하도록 자연스럽게 번역한다.
- 기술 용어는 프로젝트 용어집과 동일하게 사용한다.
- 원문에 없는 공략이나 추가 정보를 임의로 넣지 않는다.
- 검증상 미번역이 0개여도 자동 제목, fallback 제목, 아이템 `custom_name` 또는 Task 제목 때문에 영어가 표시될 수 있으므로 실제 화면 표시 경로를 검사한다.

#### 목차와 챕터 제목

- `Tools & Gear`는 `도구 및 장비`, `Storage`는 `저장소`, `Basic Storage`는 `기본 저장소`처럼 일반 분류명을 자연스러운 한국어로 번역한다.
- 공식 모드명과 프로젝트명은 원칙적으로 공식 영문 이름을 유지하고 뜻으로 직역하거나 한글로 음역하지 않는다.
- 같은 화면에서 공식 모드명이 영문, 음역과 뜻 번역으로 뒤섞인 상태를 허용하지 않는다.
- `Applied Energistics 2` 또는 `Applied Energistics 2 (AE2)`를 사용하고 `어플라이드 에너제틱스 2`는 사용하지 않는다.
- 모드명인 `Refined Storage`와 `Silent Gear`를 유지하고 `정제된 저장소`, `침묵의 장비`로 바꾸지 않는다.
- `Extended AE`와 `Advanced AE`처럼 여러 공식 모드명을 묶은 제목은 각 공식 이름을 유지하면서 조사와 접속 표현만 자연스럽게 정리한다.
- 공식 이름인지 일반 분류명인지 불확실하면 실제 모드명, 네임스페이스와 프로젝트 메타데이터를 확인하고 추측하지 않는다.

#### 퀘스트 제목

- 특정 아이템 하나를 소개하는 퀘스트 제목은 프로젝트 리소스팩에 확정된 해당 아이템의 한국어 이름과 정확히 일치시킨다.
- 퀘스트에서 아이템을 별도로 직역하거나 다른 표현으로 바꾸지 않으며 JEI, 인벤토리 툴팁과 퀘스트 제목의 이름을 통일한다.
- 아이템 이름이 아닌 창작형 제목은 짧고 자연스럽게 번역하되, 원문의 농담을 살리더라도 기능과 진행 목적을 알아보기 어렵게 과도하게 의역하지 않는다.
- 제목의 등급, 수량, 재료, 색상과 기능 구분을 보존한다.

#### Task 제목과 자동 fallback

- ItemTask의 아이템명이 리소스팩에서 정상 번역되면 단순 아이템명을 `task.title`로
  중복 지정하지 않는다.
- 영어 원본에 `task.title`이 있어도 구체적인 단일 아이템명을 그대로 반복한 제목은
  제거하고, Smart Filter·태그·복수 아이템·범주형 제목은 유지한다.
- `task.title`은 행동, 조건, 여러 아이템 묶음처럼 별도 설명이 필요한 작업에만 사용하며,
  퀘스트 자체의 `quest.title`과 구분한다.
- 퀘스트에 명시적 `title`이 없으면 첫 번째 Task 제목을 대신 쓰는지 검사한다.
- Task에 명시적 `title`이 없으면 아이템 이름이나 다른 fallback 제목을 쓰는지 검사한다.
- 언어 파일의 명시적 키만 비교하지 않고 `chapter/group title`, `quest title`, `quest subtitle`, `quest description`, `task title`, 아이템 hover name, 아이템 `custom_name` 또는 literal component와 첫 Task를 이용한 quest fallback title을 모두 확인한다.
- 자동 fallback 결과가 영어이거나 프로젝트의 확정 아이템 번역과 다르면 필요한 `quest.<ID>.title` 또는 `task.<ID>.title`을 한국어 언어 파일에 명시적으로 추가한다.
- 원본 퀘스트 구조와 JAR은 수정하지 않고 가능한 한 `ko_kr` 번역 키로 해결한다.

### 고유명사와 전문용어

- 사용자가 확정한 프로젝트 용어집을 가장 우선한다.
- 그다음으로 마인크래프트 공식 번역과 널리 정착된 한국어 표현을 참고한다.
- 모드 자체 한국어와 기존 ATM10 5.4 번역은 참고 자료일 뿐 최종 기준으로 간주하지 않는다.
- 기존 번역이 검색하기 어렵거나 프로젝트 용어집과 충돌하면 프로젝트 번역으로 교정한다.
- 모드명, 세계관 고유명사와 재료명은 의미를 추측해 새로운 이름을 만들지 않는다.
- 정착된 한국어 명칭이 없으면 원문 유지 또는 자연스러운 음역 중 하나를 선택해 용어집에 기록한다.
- 문맥에 따라 다른 번역이 필요하면 예외 사유와 적용 범위를 용어집에 기록한다.

## 산출물 경계

- 모드 언어 파일은 `working/`에서 작업하고 검증된 완성본만 `output/resourcepack/`에 배치한다.
- FTB Quests, KubeJS 등 인스턴스 경로 덮어쓰기용 파일은 검증된 완성본만 `output/overrides/`에 배치한다.
- `temp/`에는 언제든 삭제하고 다시 만들 수 있는 자료만 둔다.
- `glossary/`에는 공통 용어와 번역 금지어를 관리한다.
- `reports/`에는 조사 결과, 오류, 불확실한 항목과 작업 현황을 기록한다.
- `manifests/`에는 자동 생성된 파일 목록, 키 수, 비교 결과를 저장한다.
- 원본 JAR, ZIP, 월드, 로그와 대용량 추출물은 저장소에 포함하지 않는다.

## 코딩 스타일 및 구현 원칙

- Python은 들여쓰기 4칸, 줄 길이 최대 100자를 따른다.
- 주석, 독스트링, 로그와 사용자용 보고서는 한국어로 작성한다.
- 파일은 UTF-8(BOM 없음)으로 유지한다.
- 파일을 읽고 쓸 때 가능한 도구와 API에서 UTF-8 인코딩을 명시한다.
- 한글이 포함된 파일의 셸 출력이 깨져 보여도 파일 손상으로 단정하지 않는다.
- 파일 전체 재작성은 사용자가 명시적으로 요청했거나 파일 대부분이 실제로 변경되는 경우에만 허용한다.
- 수정 범위 밖의 코드, 주석, 포맷을 건드리지 않는다.
- 같은 의미의 값을 새로 하드코딩하지 않고 기존 상수나 헬퍼를 재사용한다.
- 이번 변경으로 사용되지 않게 된 import, 변수, 함수만 제거한다.
- 작업 전부터 존재하던 미사용 코드는 요청받지 않았다면 제거하지 않는다.

## 조사 및 자동화 스크립트

- 조사 스크립트는 JAR을 ZIP 형식으로 읽기만 한다.
- 원본 JAR을 추출, 수정, 재압축하거나 다시 쓰지 않는다.
- 경로에 한글과 공백이 있어도 동작하도록 작성한다.
- 처리하지 못한 파일은 조용히 건너뛰지 않고 오류 목록에 기록한다.
- 대용량 임시 추출물은 `temp/`에만 만들고 다시 생성할 수 있도록 한다.
- 스크립트가 실제 게임 폴더에 쓰기 작업을 수행하지 않도록 기본값을 안전하게 유지한다.
- 실제 적용용 스크립트는 사용자의 명시적 요청이 있어야 실행한다.

## 검증 규칙

각 작업 후 변경 범위에 맞게 다음 검증을 수행한다.

- JSON 또는 SNBT 문법 검사
- 번역 키 수와 누락 여부 확인
- 자리표시자, 줄바꿈, 색상 코드와 이스케이프 문자 보존 확인
- 중복 키와 잘못된 자료형 확인
- Python 파일 수정 시 가능한 경우 `ruff format .` 실행 후 `ruff check .` 실행
- 검증 명령을 실행할 수 없거나 실패하면 이유와 미검증 범위를 완료 보고에 명시
- 실제 인스턴스를 읽기 전후로 파일 경로, 크기, 수정 시각 스냅샷을 비교
- 사용자가 게임 적용을 요청하지 않은 작업에서는 실제 인스턴스가 변경되지 않았는지 확인
- 게임 적용을 수행한 경우 적용 대상으로 지정한 파일 외에 변경이 없는지 확인

새 테스트 코드는 프로젝트 계획, 기존 문서 또는 사용자의 직접 지시가 있을 때만 작성한다.  
테스트가 필요하면 기본적으로 `scripts/` 아래에 독립 실행 가능한 검증 스크립트로 작성한다.

## Git 작업 가이드라인

- 기본 작업 및 메인 브랜치는 하나로 유지하고 불필요하게 브랜치를 나누지 않는다.
- Git 작업은 이 `ATM10_Korean` 저장소 안에서만 수행한다.
- 번역, 기능 구현 또는 리팩터링이 완전히 끝난 검증 가능한 작업 단위는 자동으로 커밋한다.
- 다음 조건을 모두 충족할 때만 자동 커밋한다.
  - 요청한 번역 또는 기능이 완성됨
  - 관련 JSON/SNBT 문법 검사와 정적 검사가 통과함
  - 자리표시자, 키, 줄바꿈과 서식 코드 보존 검사가 통과함
  - 알려진 오류, 미완성, 임시 코드, 확인 필요 사항이 없음
  - 독립적으로 되돌릴 수 있는 변경 단위임
- 위 조건을 충족하지 못한 변경은 커밋하지 않고 작업 트리에 유지한다.
- 큰 작업은 하나의 거대한 커밋으로 만들지 않고, 검증 가능한 독립 작업 단위가 완료될 때마다 나누어 자동 커밋한다.
- 커밋 전 실제 스테이징 대상을 확인하고, 계획서 파일(.md), agents.md, claude.md, 사용자의 기존 변경사항은 스테이징 및 커밋에서 제외한다.
- `git push`, `git reset`, `git rebase`, `git commit --amend`는 수행하지 않는다.
- 커밋 메시지는 저장소의 기존 형식에 맞춘 짧은 현재 시제 명령형으로 작성한다.
- 자동 커밋했다면 커밋 해시와 커밋 메시지를 완료 보고에 포함한다.
- 커밋하지 못했다면 그 이유와 남아 있는 변경사항을 완료 보고에 명시한다.

## 작업 완료 보고

작업 완료 후 다음 내용을 12살도 이해할 수 있도록 번호를 매겨 간단히 설명한다.

1. 무엇을 조사하거나 번역했는지
2. 어떤 파일을 만들거나 수정했는지
3. 기존 번역 재사용 수와 새 번역 수
4. FTB Quests 또는 KubeJS 관련 작업 여부
5. 수행한 문법 및 안전 검증
6. 실제 게임에 적용했는지와 적용 경로
7. 남은 미번역 또는 수동 검토 항목

커밋하지 않은 경우 변경 내용을 요약한 추천 커밋 메시지를 한 줄로 출력한다.
